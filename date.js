const no=document.getElementById("no"),yes=document.getElementById("yes"),area=document.getElementById("area"),tease=document.getElementById("tease");let tries=0;function escapeNo(){tries++;let a=area.getBoundingClientRect(),b=no.getBoundingClientRect();no.style.left=Math.random()*Math.max(5,a.width-b.width-5)+"px";no.style.top=Math.random()*Math.max(5,a.height-b.height-5)+"px";no.style.right="auto";no.style.transform="none";tease.textContent=["Nice try 😌","Not that one ♥","It keeps escaping!","I think it wants you to press Yes 😏"][Math.min(tries-1,3)]}no.addEventListener("mouseenter",escapeNo);no.addEventListener("pointerdown",e=>{e.preventDefault();escapeNo()});no.addEventListener("click",e=>{e.preventDefault();escapeNo()});yes.onclick=()=>{
  document.getElementById("ask").classList.add("hidden");
  document.getElementById("accepted").classList.remove("hidden");
  celebrate();
  const message = encodeURIComponent("YES ❤️ I’d love to go on a coffee date with you ☕💕");
  setTimeout(() => {
    window.location.href = "https://wa.me/916398033128?text=" + message;
  }, 1800);
};function heart(){let h=document.createElement("span");h.className="heart";h.textContent=Math.random()>.25?"♥":"♡";h.style.left=Math.random()*100+"vw";h.style.fontSize=12+Math.random()*22+"px";h.style.setProperty("--drift",Math.random()*120-60+"px");h.style.animationDuration=7+Math.random()*6+"s";document.getElementById("hearts").appendChild(h);setTimeout(()=>h.remove(),14000)}setInterval(heart,650);function celebrate(){let c=document.getElementById("confetti"),x=c.getContext("2d");c.width=innerWidth;c.height=innerHeight;let p=Array.from({length:130},()=>({x:innerWidth/2,y:innerHeight*.45,vx:(Math.random()-.5)*15,vy:-Math.random()*13-3,g:.3,s:5+Math.random()*7,h:[345,15,42,320][Math.floor(Math.random()*4)]})),f=0;(function d(){x.clearRect(0,0,c.width,c.height);p.forEach(q=>{q.x+=q.vx;q.y+=q.vy;q.vy+=q.g;x.fillStyle=`hsl(${q.h} 70% 65%)`;x.fillRect(q.x,q.y,q.s,q.s)});if(f++<210)requestAnimationFrame(d)})()}